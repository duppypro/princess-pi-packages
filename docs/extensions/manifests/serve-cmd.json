{
  "name": "/serve",
  "tagline": "Secure HTTPS Server Utility for Pi",
  "description": "Serves local directories securely using SSL/HTTPS certificates in a lightweight background process.",
  "usage": [
    { "flags": "[dirs...]", "desc": "Serve specified directories (default: public, docs) on HTTPS ports." },
    { "flags": "-s, --static", "desc": "Disable live reloading and serve pure static files without code injection or file watching." },
    { "flags": "-S, --show", "desc": "Discover and display the active servers list." },
    { "flags": "-H, --hide", "desc": "Hide the active servers list from the screen." },
    { "flags": "-L, --log", "desc": "List all active servers serving directories within this worktree." },
    { "flags": "-k, --kill", "desc": "Kill all servers serving directories within this active worktree." },
    { "flags": "-k, --kill [port|dir|all...]", "desc": "Kill specific servers by port, directory, or 'all' to stop all active servers." },
    { "flags": "--help", "desc": "Display this help information." }
  ],
  "examples": [
    { "cmd": "/serve", "desc": "Serves public/ on 8080 and docs/ on 8081." },
    { "cmd": "/serve --static", "desc": "Serves public/ and docs/ statically with hot-reloading disabled." },
    { "cmd": "/serve src/ assets/", "desc": "Serves src/ on 8080 and assets/ on 8081." },
    { "cmd": "/serve -k 8080 docs/", "desc": "Kills server on port 8080 and server serving docs/." },
    { "cmd": "/serve -k all", "desc": "Instantly terminates all active dev servers across all worktrees." }
  ]
}