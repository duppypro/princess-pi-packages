#!/usr/bin/env bash
# Wrapper so `!./merge <ref>` in Claude Code behaves like `/merge <ref>` in Pi.
# bin/merge.mjs is plain ESM JS (runs under node_modules when npm-installed globally).
exec node "$(dirname "$0")/bin/merge.mjs" "$@"
