I read `extensions/lib/wtft-renderer.ts` in full (1683 lines), then the three artifacts and `CONTEXT.md`. Findings below, one line each, grouped by artifact and swept in file order for the source's own comments.

## `docs/EXT_WTFT.html`

1. "classifies each historical interaction into one of **9 work types**" (:50) — `wtft-renderer.ts:46-49` `CATEGORY_ORDER` has **14**: overhead, interrupted, plan, spec, research, web, grep, code, tests, git, agents, prompt, compaction, other.
2. "the turn is `mixed`" (:85) / "multiple categories read becomes `mixed`" (:91) — no `mixed` category exists; `wtft-renderer.ts:57-72` `CATEGORY_STYLE` has no `mixed` key, so a `mixed` classification would crash the legend map at `:1060`.
3. Classification table (:117-157) omits `plan`, `web`, `agents`, `compaction`, `interrupted`, `overhead` — all six are rendered as legend entries and bar segments at `wtft-renderer.ts:1059-1062`.
4. "Spec Work … ANSI Bright Green (`\x1b[92m`)" (:118-120) — `wtft-renderer.ts:59` uses 256-colour `fg: 117` (light blue), emitted as `\x1b[38;5;117m` at `:1061`.
5. "Code Work … ANSI Orange (`\x1b[38;5;208m`)" (:123-125) — `wtft-renderer.ts:63` uses `fg: 179` (tan); 208 is used only for surge highlighting (`:1212-1217`).
6. "Tests Work … ANSI Yellow (`\x1b[93m`)" (:128-130) — `wtft-renderer.ts:64` uses `fg: 149` (green).
7. "Research Work … ANSI Magenta (`\x1b[95m`)" (:133-135) — `wtft-renderer.ts:60` uses `fg: 141`.
8. "Git Work … ANSI Cyan (`\x1b[96m`)" (:138-140) — `wtft-renderer.ts:65` uses `fg: 110`.
9. "Grep Work … ANSI Blue (`\x1b[94m`)" (:143-145) — `wtft-renderer.ts:62` uses `fg: 68`.
10. "Prompt Work … ANSI Dim White (`\x1b[37m`)" (:148-150) — `wtft-renderer.ts:67` uses `fg: 216` (peach).
11. "Other Work … ANSI Dark Grey (`\x1b[90m`)" (:153-155) — `wtft-renderer.ts:71` uses `fg: 245`.
12. Legend examples render `░` for Prompt/Other and `▒` for Mixed (:181, :190) — `wtft-renderer.ts:58-71` sets `char: "█"` for every category; `░` and `▒` are never emitted anywhere in the file.
13. Legend examples order "Spec Mixed Code Tests Research Git Grep Prompt Other" (:181, :190) — `wtft-renderer.ts:1059` iterates `CATEGORY_ORDER`, so the legend actually begins `Ovrhd Waste Plan Spec …`.
14. "Legend indicators are placed dynamically on the top title row (or on the second row if the terminal width is narrow)" (:175) — `wtft-renderer.ts:1065-1070` always pushes the legend as its own row 1; the comment at `:1066-1068` states the dynamic placement was deliberately removed to stop layout flip-flop.
15. "renders a stacked, color-coded **ASCII** bar graph" (:50) — `wtft-renderer.ts:374, 377, 1245-1249, 1302, 1308` emit Unicode block elements `█ ▌ ▃ ▇`, not ASCII.
16. Cumulative mode "guaranteeing **monotonically non-decreasing** bar widths — a category's segment can only grow, never shrink" (:172) — `wtft-renderer.ts:1116-1118` clamps only categories whose previous allocation was ≥ 2 half-slots, and `:1124-1133` actively decrements clamped categories when the clamp overshoots, so segments can shrink.
17. Cumulative mode "Each category's segment is allocated characters from its absolute cumulative cost against the global scale-maximum" (:172) — `wtft-renderer.ts:1103` computes `(bin.costs[cat]/scaleMax) * halfSlotWidth` where `halfSlotWidth` (`:1096-1097`) is *already* scaled by `total_cost/scaleMax`, so the floor pass divides by `scaleMax` twice and systematically under-allocates.
18. Same claim (:172) — the fill pass at `wtft-renderer.ts:1148` uses a different denominator (`bin.costs[cat]/bin.total_cost`) than the floor pass at `:1103` (`/scaleMax`); the final split is a blend of two inconsistent bases.
19. Bucket mode "characters are plotted in the reverse priority of the legend (from right to left, i.e. `Other` -> `Spec`) ensuring that more critical categories like `Spec` always overlay" (:173) — `wtft-renderer.ts:1305` sorts colliding entries by **cost descending**; legend priority is never consulted, so a large `Other` overlays a small `Spec`.
20. Bucket mode "placing exactly **one colored marker block**" per category (:173) — `wtft-renderer.ts:1296-1309` renders a two-category cell as `▌` (fg + bg), so a collision yields one glyph carrying two categories, not one block each.
21. Bucket mode collision handling (:173) is silent on loss — `wtft-renderer.ts:1305-1308` keeps only the top **2** entries per position; a third or later category at the same index is silently dropped.
22. "Scaling & Layout … typically capped at 30-40 characters wide" (:201) — `wtft-renderer.ts:1013-1020` sets `maxBarWidth = max(width, 40) - prefixWidth - tickReserve`, which at the documented default width 240 produces bars far wider than 40.
23. "Normalizes the absolute length of each interval's ASCII bar based on **the maximum cost in any single interval**" (:201) — true only in bucket mode; `wtft-renderer.ts:968-969` uses `totalSessionCost` (all bins, including ones outside `limit`) as the scale in cumulative mode.
24. "`-w, --width <number>` … dynamically scaled down to active terminal columns" (:60) — `wtft-renderer.ts:1013` re-raises the width with `Math.max(width, 40)` *after* the terminal clamp at `:816`, so output overflows terminals narrower than 40 columns.
25. "Width is always auto-detected from terminal (clamped at 1023). **No `-w/--width` flag.**" (:306) — contradicts both `wtft-renderer.ts:794` (`width?: number` opt), `:815-816` (honoured), and this same file's own `-w, --width` documentation at :60; no 1023 clamp exists anywhere in the source.
26. "Badges: … `⚡ SURGE ENDING` (within 20 min of end, blinking green)" (:163) — unreachable: `wtft-renderer.ts:680` returns `'surge'` for any minute inside the window, so the `'ending'` branch at `:686-688` can never fire and the badge at `:754-755` never renders.
27. "the ticks scale and dollar labels are collapsed into a single … row … The dollar labels themselves (e.g. `$0.00`, `$scaleMax/2`, and `$scaleMax`)" (:176) — `wtft-renderer.ts:499` emits **five** ticks (0, ¼, ½, ¾, max); the quarter ticks are undocumented.
28. Same (:176) — silent on suppression: `wtft-renderer.ts:477` returns `null` when `barWidth < 15`, so the entire tick row vanishes with `--ticks` still enabled.
29. "rendered as `─── Mmm-DD ───` leading directly into the dollar scale line" (:176) — `wtft-renderer.ts:1074` builds `── ${titleDateStr} ` (two dashes, one trailing space, then `─` padding); three-dash form is never produced.
30. Day-boundary separator example `── Jun-19 ─────…` (:193) — `wtft-renderer.ts:1181-1185` inserts `┼` at five tick positions in every divider, including day dividers, so the line is not unbroken.
31. Day-change separator rows are documented unconditionally (:176, :193) — `wtft-renderer.ts:1195` gates them on `showTicks`, so `--no-ticks` silently removes date separators too.
32. "The timeline uses `─` box-drawing horizontal rule characters (U+2500) for a **continuous unbroken line**" (:161) — `wtft-renderer.ts:736, 747-748` splice double-width clock/sun/moon emoji into the 24-cell run, breaking both continuity and column alignment.
33. "The current hour displays a clock-face emoji (`🕐`–`🕛`, hour % 12)" (:162) — `wtft-renderer.ts:735` indexes `CLOCK_FACES` with `h % 12` where index 0 is `🕛`, so hour 0 renders `🕛` and hour 1 renders `🕐`; the stated range order is inverted at the wrap point.
34. Timeline/badge section is silent on emoji suppression — `wtft-renderer.ts:718-759` never receives or consults `disabledEmoji`, so `--no-emoji` leaves the moon, clock, sun and `⚡` glyphs in place (`:1217`, `:751-755`).
35. "The Pi widget passes the session model explicitly; the CLI auto-detects DeepSeek from **classified interactions**" (:167) — `wtft-renderer.ts:1047-1051` takes the **first** interaction that has any `model` field and stops; classification is not involved, and a session that starts on Claude then switches to DeepSeek renders an all-green timeline.
36. "`-t, --tz, --timezone <zone>` … to bypass UTC systems" (:61) — silent on failure: `wtft-renderer.ts:191-196` swallows an invalid zone and falls back to system local with no warning.
37. "Cache Miss divider … `── Cache Miss ──` divider line" (:164) — the rendered label is `── Cache Miss ` followed by `─`/`┼` fill (`wtft-renderer.ts:1170-1187, 1203`); there is no trailing `──` delimiter after the label.
38. "Config Hierarchy … Code defaults — hardcoded (interval=1h, limit=10, mode=cumulative, showTicks=true, timezone=America/Los_Angeles, disabledEmoji=false)" (:243) and Config Format (:251-258) omit `width` — `wtft-renderer.ts:785` makes `width` a **required** field of `defaultSettings`.
39. Config Format (:251-258) has no `unit` key — `wtft-renderer.ts:802, 810` reads `unit` only from `opts`, never from `defaultSettings`, so token-mode cannot be persisted (see manifest finding 55).
40. Token-unit mode is documented **nowhere** in this file — `wtft-renderer.ts:1219-1271` renders an entirely different bar (block-height `▃`/`▇`), `:1347` a key bar, `:1349-1352` a `↑/↓/R/CH` footer, and `:1036-1037` a different title (`🔢 WTF Tokens?`).
41. The per-model token summary table is documented nowhere — `wtft-renderer.ts:1548-1679` renders headers, per-model cache/think detail lines, a TOTAL row, an unpriced-model `?` legend, and a compaction summary.
42. The "Other bloat" warning is documented nowhere — `wtft-renderer.ts:1329-1341` emits `⚠️ "Other" category: N% of session cost …` whenever Other exceeds 20% **and** $6.00.
43. The `CH: … cache hit (… read / … total ops)` footer line is documented nowhere — `wtft-renderer.ts:1360-1366` (cost mode) and `:1354-1357` (token mode).
44. `--other` "descending-frequency histogram" (:72) — `wtft-renderer.ts:1494-1501` orders **groups** by a fixed `groupOrder` array, not by frequency; only commands *within* a group are count-sorted (`:1517`).
45. `--other` example commands "node, npm, **cat**, mkdir" (:72) — `cat` is absent from every set in `SEMANTIC_GROUPS` (`wtft-renderer.ts:1374-1407`), so it falls to `"Unclassified"` at `:1472`.
46. `--other` "their execution counts, and **cumulative costs**" (:72) — `wtft-renderer.ts:1454-1460` adds the **whole** interaction cost once per command extracted from that interaction, so a turn running three commands triple-counts its cost into the group totals.
47. `--other` output is silent on the empty case — `wtft-renderer.ts:1464-1466` returns the sentence `No 'Other' commands found in this session.` instead of a histogram.
48. `--other` (:72) and the manifest demo omit the header — `wtft-renderer.ts:1503` always prefixes `--- 'Other' Command Histogram ---`.
49. "Recursive Subagent Rollup … blends their interactions chronologically based on their individual timestamps" (:202) — `wtft-renderer.ts:841-891` bins in **array order** and never sorts by timestamp; `turnIndex` (`:842`) and therefore all turn-based bins depend on that unsorted order.
50. "Binning Engine: Groups entries by their `timestamp` … based on the requested interval" (:200) — incomplete: `wtft-renderer.ts:226-229` supports a turn-based mode that ignores timestamps entirely.
51. Cache Miss divider "in turn-based intervals as well as time-based ones" (:164) is the only mention of turn-based intervals in the whole document, yet the Options list (:58) never offers a `t` interval unit — see finding 52.

## `docs/manifests/wtft-cmd.json`

52. `"-i, --interval <size><m|h|d|w>"` (:114) — `wtft-renderer.ts:157` also accepts `<n>t`, `<n>turn`, `<n>turns` (turn-based binning, `:226-229`); a reader concludes turn intervals are unsupported.
53. Same (:114-115) — silent on invalid input: `wtft-renderer.ts:162` returns `{size:1, unit:"h"}` for anything unparsable (e.g. `-i 0h`, `-i 5x`) with no error.
54. `"description": "Displays a horizontal, **stacked** TUI bar chart"` (:5) — only cumulative cost mode stacks; `wtft-renderer.ts:1281-1311` renders bucket mode as a scatter of isolated markers with gaps.
55. `"--tokens … shows a per-model token summary table with utilization. … Config-persistable."` (:194-195) — `wtft-renderer.ts:810` reads `unit` from `opts` only, never from `defaultSettings`, so nothing in the renderer honours a persisted unit; and `--tokens` additionally re-renders the whole chart (`:1219-1271`), not just a table.
56. `"activity (Spec, Code, Plan, Agents, ...)"` (:5) — the elided list hides `Ovrhd`, `Waste` and `Cmpct`, which `wtft-renderer.ts:46-49` places **first** in bar and legend order; the description's "every turn's cost includes the context re-send overhead regardless of category" also sits oddly against `overhead` being its own stacked category (`:70`).
57. `"-l, --limit <number>"  "Limit the number of interval bars displayed (default: 10)"` (:118-119) — `wtft-renderer.ts:961-962` keeps the **newest** N after reversing, while the cumulative scale at `:968-969` still uses the full-session total, so limiting shrinks every visible bar.
58. Same (:118) — silent on the empty case: `wtft-renderer.ts:964-966` returns `null` (no output at all) when zero bins survive.
59. `"-w, --width <number>" … "(default: 240)"` (:130-131) — `wtft-renderer.ts:1013` floors the effective width at 40 regardless, and `:816` caps it at the detected terminal width; neither bound is stated.
60. `"--ticks, --no-ticks … proportional cost scale tick marks above the bars"` (:142-143) — `wtft-renderer.ts:1195` also gates **day-boundary dividers** on this flag; `--no-ticks` removes date separators, which the description does not mention.
61. `"--emoji, --no-emoji … When disabled, clean single-width ASCII characters are used instead."` (:158-159) — `wtft-renderer.ts:1037-1038` substitutes only the title glyph; the timeline emoji (`:735-736, 747`), the surge bolt `⚡` (`:1217`) and the warning `⚠️` (`:1339`) remain, and the substitutes `[#]`/`[$]` are three cells wide, not single-width.
62. `"--by-model" … "Models missing from the pricing registry are marked '?' (priced at default rates)"` (:198-199) — incomplete: `wtft-renderer.ts:1659, 1661-1663` also stamps `?` on the **TOTAL** row and appends `? = model not in pricing registry — priced at default $3/$15 rates; totals may be unreliable`.
63. `--by-model`/`--tokens` (:194-199) — silent on exclusion: `wtft-renderer.ts:1566-1570` drops every `(unknown)` and `<synthetic>` interaction from the table, so its TOTAL cost is lower than the chart's session cost, reported only as `(N untagged interactions skipped)` (`:1598`).
64. `--tokens` (:194) — silent on the compaction line: `wtft-renderer.ts:1665-1676` appends `Compaction: N event(s), X total tokens freed`.
65. `"--tz, --timezone <zone>"` (:134) omits `-t`, yet the examples use `/wtft -t America/New_York` (:228) — the manifest contradicts itself; the renderer takes only a resolved zone string (`:819`).
66. Example `"/wtft -i 4h -l 20"  "Show last 20 4-hour interval bars of **cumulative token costs**"` (:232-233) — `wtft-renderer.ts:810` defaults `unit` to `"cost"`; without `--tokens` these bars are dollars, not tokens.
67. `--other` result "Unclassified commands appear under their own section" (:63) with demo showing `cat` under `[System & File Utilities]` (:74-76) — contradicts itself against `wtft-renderer.ts:1397` (no `cat` in the `sys` set) → `cat` lands in `Unclassified`.
68. All chart demos (:15-17, :28-30, :41-42, :53-54, :88-89) use colours 108/173/238/134/168/73/67 and glyphs `░`/`▒` — `wtft-renderer.ts:57-72` defines none of those colour indices and `:374/377/1302/1308` emit only `█`/`▌`.
69. Demo `"── Jul-01 ──────…"` day header (:14, :27, :87) — `wtft-renderer.ts:1181-1185` inserts `┼` tick markers into every divider line.
70. Demo bucket rows `"00:00  $3.45  ████████"` show contiguous bars in `-b`-style output (:28-30) — `wtft-renderer.ts:1296-1300` pads non-spending positions with spaces, producing sparse markers.
71. `"--thinking-budget <n>" … "utilization percentage display"` (:190-191) — silent on the fallback: `wtft-renderer.ts:1638-1639` prints a bare `Think: N tokens` line whenever the budget is absent or zero.

## `README.md`

72. "`/wtft` | … A cost-auditing widget that hooks into turn-completion events to display running session costs" (:29) — incomplete against `wtft-renderer.ts:800-805`: the same entry point also renders a token-unit mode, a DeepSeek surge timeline, and a session-name suffix.
73. "Driven by one shared manifest (`docs/manifests/*-cmd.json`) so `--help` renders identically everywhere" (:17-18) — the manifest's `-i <size><m|h|d|w>` (`wtft-cmd.json:114`) cannot describe the turn unit the renderer accepts at `wtft-renderer.ts:157`, so identical rendering still yields incomplete help.
74. "3. Test the search tool: ```/wtft```" (:100-103) — `/wtft` is the cost widget, not the search tool; the DDG search tool is `search_web` per this same table (:30). No renderer line supports this; the step is simply mislabelled.

## Source file's own docstrings and banner comments (file order)

75. `@description Bar chart rendering, histograms, token summaries, and terminal utilities … Builds visual output from parsed Interaction arrays` (`:1-7`) — the module also owns interval-string parsing (`:150`), timezone/ISO-week arithmetic (`:169, :199`), surge-window math (`:648, :674`) and moon-phase computation (`:707`), none of which build visual output from interactions.
76. `key?: string; // ISO bin key (e.g. "2026-07-15T18:00")` (`:19`) — `wtft-renderer.ts:235, 242` emit seconds (`…T18:00:00`), and `:228` emits `turn:000010` for turn mode, which is not an ISO key at all.
77. Banner `TOKEN-UNIT MODE (#14): Background colors and rendering` (`:35-37`) — has no body; the next statement is the unrelated `CATEGORY_ORDER` banner at `:39`, and the `bg` field it refers to (`:57-71`) is never read anywhere in the file.
78. `Legend, cost-mode stacking, bucket-mode markers, and token-mode segments **all derive from this table, so bar order matches legend order by construction**` (`:41-43`) — bucket mode's collision order is decided by cost at `:1305`, not by `CATEGORY_ORDER`.
79. `char = bar glyph (█ for cost-mode)` (`:52-53`) — `.char` is read only by the legend at `:1061`; cost-mode bars hardcode `█`/`▌` at `:374, 377, 1302, 1308` and never consult it.
80. `fg = legend/bar ANSI 256 color` (`:51`) — undocumented second role: `:1307-1308` uses another category's **fg** as the ANSI **background** in collision cells.
81. `label: string | null` with `:1060` filtering `label !== null` — no category has a null label (`:58-71`), so the filter is dead and the nullable type is unreachable.
82. `Compute total tokens for an interaction (**mirrors Anthropic API fields — mutually exclusive, no double-count**)` (`:79-81`) — `:83` adds `reasoningTokens`, which is not an Anthropic usage field at all (`wtft-parser.ts:281` reads `usage.reasoning_tokens`); where a provider reports it inside `output_tokens` the sum double-counts.
83. `Compute Pi-style footer summary line. ↑47k ↓14k R1.3M **CH99.7%**` (`:86-89`) — `:103` uses `toFixed(0)`, so a fractional hit rate can never render; that example would print `CH100%`.
84. Same docstring's `↑` (`:88`) — `:93` folds `cacheReadTokens + cacheWriteTokens` into `input`, so the up-arrow is total input including cache, not uncached input.
85. `const totalCacheOps = cr + cw + input - cr - cw; // input-only portion...` (`:99`) — assigned and never read; the expression also reduces to `input`, not an "input-only portion".
86. `// input already includes cache? No — field layout: inputTokens = non-cached …` (`:101`) — contradicted two lines up by `:93`, which does add `cr + cw` into `input`; the very next comment (`:102`) then says `= input (total input including cache)`.
87. `Call this inside the interaction→bin loop in **binInteractions()**` (`:113-115`) — no `binInteractions` function exists; the loop lives in `buildWtftLines` at `:841-891`.
88. Banner `SHARED FILE PARSER (#54 DRY refactor) — Single source of truth for reading a .jsonl session file into Interaction[] … the parseEntryToInteraction call and subsequent dedup are identical — those live here` (`:137-141`) — no file reading, no `parseEntryToInteraction`, and no dedup implementation exists in this file; `parseEntryToInteraction` is defined in `wtft-parser.ts:154`.
89. `Parse a .jsonl session file into raw (undeduped) interactions. … @param filePath - Absolute path to the .jsonl session log @returns Array of parsed interactions` (`:143-149`) — **misattached**: the very next symbol is `export function parseInterval(val: string): IntervalConfig` (`:150`), so TypeScript/IDE hover shows this docstring for the interval parser, which takes an interval string and returns an `IntervalConfig`.
90. `parseInterval` itself (`:150-163`) carries no accurate doc — its turn-unit branch (`:157`) and its silent `1h` fallback (`:162`) are undocumented everywhere.
91. Banner `COMMAND NORMALIZATION (#63) — Strips cd /path prefixes and VAR=value assignments …` (`:165-167`) — sits directly above `getZonedParts` (`:169`), a timezone function; `normalizeCommand` is imported from `wtft-shared.js` (`:12`) and not defined here.
92. `// Turn-based binning (#121) … Label = highest turn # in bucket` (`:224-225`) — `:228` actually labels with the bucket **ceiling** (`binEnd`), and `:869` then overwrites it with the highest turn actually seen; the two disagree for a partially filled final bucket.
93. `distributeChars` (`:261`) — exported and reachable only from tests; the render path uses the inline allocator at `:1096-1165`, the bucket collision map at `:1284-1294`, or the token segment loop at `:1226-1252`. Never called in-file.
94. `Distribute proportional counts across barWidth × 2 half-slots … for half-block rendering` (`:307-311`) — `distributeHalfSlots` (`:312`) is likewise never called by the half-block render path; `:1096-1165` reimplements the distribution with clamping instead.
95. `@param halfSlots - Array of category tags, length = barWidth × 2` / `@returns ANSI string for the bar (barWidth cells wide)` (`:360-362`) — `:373` explicitly tolerates an odd-length array (`right === undefined`), and output width is `ceil(length/2)`, so neither invariant holds.
96. `Build tick line with token-count labels (e.g. "0.0", "25.0k", "50.0k")` (`:409`) — `:438-439` only inserts `.0` when `formatTokenCount` produced no dot, so a raw count like 500 renders as ` 500.0 ` — a token count with a spurious decimal, not one of the documented forms.
97. `Same layout as buildTickLine` (`:410`) — `:1019` gives token mode a 5-char tick reserve vs cost mode's 3, so the two do not lay out identically.
98. `Adaptive precision: 4 decimal places for sub-cent values (< $0.01), 2 decimal places otherwise` (`:559-561`) — `:562` guards on `cost > 0`, so `$0.00` and any negative cost get 2 decimals, not 4.
99. Same docstring (`:558-564`) — `renderOtherHistogram` bypasses it entirely, hardcoding `toFixed(4)` at `:1513` and `:1521`, so histogram costs never use the adaptive rule.
100. Banner `// MAIN LAYOUT COMPILER` (`:591`) — sits above `getTerminalWidth` (`:593`), a width-probe helper; the actual layout compiler `buildWtftLines` starts 189 lines later at `:780`.
101. `getTerminalWidth(isWidget = false, **disabledEmoji = false**)` (`:593`) — the `disabledEmoji` parameter is never referenced in the body (`:594-620`), yet `:814` passes it, implying emoji affect width computation. They do not.
102. `Widgets: subtract minimal breathing room (1 char per side)` (`:617-619`) — accurate for `:620`, but silent that the tmux/`tput` probes at `:603-616` fire only when width is exactly 80, so a genuine 80-column terminal pays two `execSync` calls on every render.
103. `Build a 24-hour surge timeline string in the format: (---[colored]---◆---) [⚡ SURGE 2x] … @param currentHour - Current local hour (0-23) for **diamond marker**` (`:693-700`) — **misattached**: the next symbol is `const MOON_PHASES` (`:702`), so TypeScript binds this docstring to the moon-phase array, not to `buildTimelineString` (`:718`), which is left undocumented.
104. Same docstring's format string (`:694-695`) — no `◆` diamond and no surrounding parentheses are ever emitted; `:736` renders clock-face/`☀️`/`─` and `:748` wraps the body in moon emoji instead.
105. `// Reference new moon: 2026-07-14 09:43 UTC. **Re-centre every ~2 years.**` (`:704`) — nothing in the code detects or warns about drift; `:705` is a fixed constant with no staleness check.
106. `Checks current surge proximity (in UTC). Returns status and multiplier.` (`:673`) — silent that `multiplier: 2.0` is returned for `'approaching'` (`:684`) when no surcharge is yet in effect, and that callers ignore it entirely (`:1055-1056` reads only `.status`).
107. Same (`:673`) — silent that the `'ending'` return at `:687` is unreachable behind the `'surge'` return at `:681`.
108. `// Current hour → clock face emoji (never at noon — ☀️ owns position 12).` (`:734`) — accurate for `:736`, but silent that the emoji is double-width, so the "24-hour" bar occupies 25+ terminal cells whenever the current hour is not noon.
109. Banner `CACHE EFFICIENCY HELPER (#79) … **Returns undefined if no cache-related tokens are present**` (`:761-764`) — `:772-773` returns undefined only when `cr + cw + input === 0`; a cache-free session with input tokens returns a defined `{hitRate: "0"}` and renders a misleading `CH: 0% cache hit` line at `:1364`.
110. Same banner's formula label `totalOps` (`:765, 775`) — the value is a **token** count, but `:1356` and `:1364` render it as `(… read / … total ops)`, mislabelling tokens as operations.
111. `@param model - Model ID for SURGE timeline coloring … Auto-detected from interactions if omitted` (`:800`) — `:1047-1051` takes the first interaction carrying any model and breaks; mixed-model sessions are detected by whichever model happens to appear first in array order.
112. `@param sessionNameSuffix - Session filename; **last 4 chars** shown as a dim title suffix` (`:805`) — `:1041` strips a trailing extension first via `/.jsonl$/`, whose unescaped `.` also matches e.g. `xjsonl`, then takes the last 4 chars of the stripped name.
113. `@param unit - Unit for bar scaling: "cost" (default) or "tokens"` (`:802`) — incomplete: `unit` also switches the title glyph (`:1036-1038`), the tick-label formatter (`:1078-1080`), the tick reserve (`:1019`), the scale rounding (`:971-972`), the footer block (`:1344-1358`) and the bloat warning (`:1330`).
114. `// Deduplicate by message.id before binning (#54) … Summing per line inflates costs ~1.8×` (`:823-825`) — accurate for `:826`, but silent that `renderTokenSummary` dedups **again** at `:1550`, so a caller passing already-deduped input pays the cost twice.
115. `// Turn-based mode (#121): always update label … and dateStr to latest interaction's date` (`:866-867`) — silent that `turnIndex` (`:842`) counts array position, not chronological order, so unsorted input yields turn numbers that do not match time order.
116. `// Server-side tool cost is a separate line item, not token spend (#73). Attribute it to the "web" category` (`:879-880`) — accurate for `:881-885`, but the `web` category it feeds appears in no artifact: neither the HTML classification table (:117-157) nor the manifest description (:5) mentions it.
117. `// Helper: build a tick-aligned divider line … reused for **cache TTL expiry** (#106)` (`:1168-1169`) — the code it serves labels the divider `Cache Miss` (`:1203`) and the comment 33 lines later (`:1200-1201`) explicitly rejects the TTL framing; the two comments contradict each other.
118. `const barMax = Math.max(0, maxBarWidth - 2); // 2-char safety margin for Pi TUI padding` (`:1221`) — token bars are 2 cells shorter than the tick line built with `maxBarWidth` (`:1079`), so token-mode tick labels do not align with the bars they scale.
119. `// Use precomputed half-slots directly (#109)` with `precomputedHalfSlots.get(bin)!` (`:1276-1278`) — the map is only populated when `unit === "cost"` (`:1092`); reaching this branch in token mode with an unpopulated `bin.tokens` dereferences `undefined` at `:1279`.
120. `// Clamp to previous bin — only for categories with ≥ 2 half-slots … Tiny 1-half-slot allocations … are allowed to flicker` (`:1109-1112`) — silent that the overshoot trim at `:1124-1133` can also reduce clamped (≥2) categories, defeating the stated guarantee.
121. `// Distribute remaining half-slots to categories furthest below their ideal proportional share.` (`:1141-1143`) — `:1148` divides by `bin.total_cost`, which is 0 for a zero-cost bin, yielding `NaN` deficits and an immediate `break` at `:1159` that leaves the bar short.
122. `// ⚡ is double-width — it fills the 2-char column gap on its own, numbers stay aligned.` (`:1216`) — assumes a double-width `⚡` render; `getVisualLength` at `:586-589` is not applied to this substitution, so terminals rendering `⚡` single-width shift the whole row by one cell.
123. Banner `SEMANTIC COMMAND SUB-CLASSIFICATION — Maps bare command names to semantic groups for wtft-other histogram` (`:1371-1372`) — silent that `getSemanticCommandGroup` (`:1409`) additionally prefix-matches full command strings (`:1415-1425`) after the bare-name lookup fails.
124. `const base = command.split("/").pop() || command; // Strip path prefix e.g. /usr/bin/ls → ls` (`:1410`) — silent that callers pass only the first whitespace-delimited token (`:1444-1445`), so the prefix branches at `:1415-1425` (`"npm "`, `"git "`, …) can never match from `renderOtherHistogram`.
125. `break; // Only capture the first effective command` (`:1448`) — the break exits the **line** loop, not the command loop (`:1437`), so a turn with several tool calls contributes one entry per call, which is what drives the cost multiplication in finding 46.
126. Banner `TOKEN SUMMARY TABLE (per-model, deduped) — Renders token counts … Wire via --tokens flag (CLI) or /wtft --tokens (Pi TUI)` (`:1533-1535`) — sits above `formatTokenCount` (`:1537`) and `shortenModel` (`:1543`); the function it describes, `renderTokenSummary`, begins at `:1548` and carries no docstring of its own.
127. `renderTokenSummary(interactions, **maxWidth** = 80, …)` (`:1548`) — `maxWidth` bounds only the separator rule at `:1595`; the header and data rows at `:1601-1626` are built from `modelColW + 6 × numColW` and routinely exceed it.
128. Separator width `modelColW + numColW * 5 + 24` (`:1595`) — there are **six** numeric columns (`:1602-1608`), so the rule is one column short of the table it underlines.
129. `// Rows — a "?" on the cost marks a model priced at fallback defaults (no registry entry, no legacy branch): the figure is a guess (#140).` (`:1611-1612`) — accurate for `:1625`, but silent that the same marker is applied to the TOTAL row at `:1659` whenever *any* single model is unpriced.
130. Trailing banner `WATCH MODE: tail -f style live re-rendering (#45) — Watches a .jsonl session file for changes and re-renders in-place.` (`:1681-1683`) — the file ends at `:1683`; no watch implementation follows, and nothing in this module watches a file.

## `CONTEXT.md` glossary

131. `CONTEXT.md` has exactly one glossary section, `## Language — Serve` (`CONTEXT.md:5-117`); **there is no section for wtft**, so no `_Avoid_` ruling governs this tool's vocabulary and I am inventing none.
132. For completeness of that check: I extracted every user-facing string the renderer emits — `Cache Miss` (`:1203`), `cached/carryover` / `new/uncached` / `$ = cost-only (web tools)` (`:1347`), `CH: … cache hit … total ops` (`:1356, 1364`), the `⚠️ "Other" category …` warning (`:1339`), `--- 'Other' Command Histogram ---` (`:1503`), `No 'Other' commands found in this session.` (`:1465`), the eight `SEMANTIC_GROUPS` labels (`:1374-1407`), `Unclassified` (`:1472`), `── Token Summary (per model, deduped) ──` (`:1598`), the column headers (`:1602-1608`), `? = model not in pricing registry …` (`:1662`), `Compaction: N event(s) …` (`:1675`), and the 14 legend labels (`:58-71`) — and none uses a term the Serve section marks `_Avoid_` (Service, daemon, listener, Dev server, Slug, Deploy, Probe, Panel, etc.). No glossary violation to report from the source.
133. Adjacent, not a ruling: `EXT_WTFT.html:199, 283, 305, 307` uses **daemon** and **Log Parser** interchangeably for the same process — the Serve section marks *daemon* `_Avoid_` only for its own **Server instance** term, and the tenancy narrowing at `CONTEXT.md:95-97` reserves *Service*, not *daemon*; with no wtft section, this is unresolved vocabulary rather than a violation.

## Outside the renderer's authority (flagged, not adjudicated)

134. `EXT_WTFT.html:199` says tag files are `wtft-tags/*.tag.v2.3.4.jsonl` while `EXT_WTFT.html:166` says `tagger v2.7.0` — the two artifact statements conflict; `wtft-renderer.ts` references neither and cannot settle it.
135. `wtft-cmd.json:119` and `EXT_WTFT.html:59` both give `limit` default 10, but `bin/wtft.ts:162` loads a default of `100`; the renderer takes `limit` from its caller (`:809`) and asserts no default.

**How I verified:** read all 1683 source lines top to bottom in two passes; enumerated `CATEGORY_ORDER`/`CATEGORY_STYLE` against the HTML table and legend examples; traced every `opts`/`defaultSettings` field from `:780-819` to its consumers; hand-executed `checkSurgeProximity` over both windows to prove the `'ending'` branch unreachable; hand-executed the cumulative half-slot allocator (`:1096-1165`) to confirm the double `scaleMax` division and the shrink path; cross-checked `Category` and `Interaction` against `extensions/lib/wtft-parser.ts:30-90, 281`; grepped the repo to confirm `distributeChars` has no caller outside tests and that `.char` is read only at `:1061`; and read `CONTEXT.md` in full to confirm the absence of a wtft section. Nothing was fixed.
