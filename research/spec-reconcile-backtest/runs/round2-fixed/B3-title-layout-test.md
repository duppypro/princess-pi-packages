I read `extensions/lib/wtft-renderer.ts` and the production code it calls (`wtft-parser.ts`, `wtft-cost.ts`, `wtft-cli-shared.ts`, `wtft-daemon-lib.ts`, `config.ts`, `session-selector.ts`, `bin/wtft.ts`, `extensions/wtft.ts`), then audited the three artifacts against it. I also ran the test (`26 passed, 6 failed`) and the built CLI on a fixture to confirm the rendered output empirically.

## tests/wtft-title-layout.test.ts

1. `"Invariant: the SURGE timeline (---◆---) MUST be on the title row"` (line 10) — `extensions/lib/wtft-renderer.ts:736` — the timeline glyphs are `─`, a clock-face emoji, and `☀️` at noon, bookended by a moon phase (`:746-748`); `◆` appears nowhere in the renderer, and `grep -c ◆ bin/wtft.mjs` returns 0.
2. `check(\`${label}: timeline on title row (contains ◆)\`, titleRow.includes("◆"))` (line 130) — `extensions/lib/wtft-renderer.ts:718-748` — asserts a glyph the production code never emits; this assertion fails 6/6 times when run (verified: `FAIL ... timeline on title row (contains ◆)` at 60/120/240 cols, cost and tokens).
3. `check(\`${label}: timeline NOT on own row\`, !timelineOnOwnRow)` (line 140) — `extensions/lib/wtft-renderer.ts:736` — passes vacuously: the loop searches rows 2+ for `◆`, which is never emitted on any row, so the check can never fail regardless of layout.
4. `"Legend goes to its own row when too wide"` (line 11) — `extensions/lib/wtft-renderer.ts:1069-1070` — the legend is pushed to its own row unconditionally (`widgetLines.push(legendStr)`), with no width test; the header's conditional contradicts both the code *and* the test's own assertion at line 149-150 ("Legend ALWAYS on its own row").
5. `"across all code paths (CLI cost, CLI tokens, CLI --watch)"` (lines 6-7) — `tests/wtft-title-layout.test.ts:117-120` — `CASES` contains only `CLI cost` and `CLI tokens`; `--watch` is never invoked, and its renderer (`extensions/lib/wtft-daemon-lib.ts:718`) is never entered.
6. `"Strictly validates title row layout consistency across all code paths"` (lines 5-6) — `extensions/wtft.ts:224` — the Pi widget path (`buildWtftLines(ctx, pi, buildOpts)`) is a separate caller that is never exercised; only the CLI binary is.
7. `const allArgs = ["-i", "1h", "-l", "2", "-w", String(columns), ...]` (line 95) — `bin/wtft.ts:456` and `:465` — the CLI ignores `-w` entirely (`width: Math.min(paddedWidth, 1023)` derived from `getTerminalWidth()`); `opts.width` is never read in `bin/wtft.ts`, so the widths under test come solely from the `COLUMNS` env var set at line 98.
8. `"Row 0 = session path, row 1 = title, row 2 = legend (if own row)"` (line 103) — `extensions/lib/wtft-renderer.ts:1069-1070` — row 2 is *always* the legend; the parenthetical implies a variant layout that does not exist.
9. `"Fixture: deepseek session so SURGE timeline has colored segments (longer visual length — exercises the overflow case)"` (lines 40-42) — `extensions/lib/wtft-renderer.ts:728-746` — the timeline is always 24 cells plus two moon bookends whatever the model; DeepSeek only changes ANSI colors, not visual length, and there is no overflow branch to exercise since the title/legend split at `:1069-1070` is unconditional.
10. `"Tests against the BUILT bin/wtft.mjs — the end-user artifact"` (lines 7-8) — accurate; `tests/wtft-title-layout.test.ts:20` resolves `bin/wtft.mjs` and `:96` executes it. Listed for completeness, not a finding.

## docs/manifests/wtft-cmd.json

11. `"flags": "-i, --interval <size><m|h|d|w>"` (line 114) — `extensions/lib/wtft-cli-shared.ts:206` — the parser also accepts `<n>t` / `<n>turn` / `<n>turns`, and `extensions/lib/wtft-renderer.ts:226-229` implements turn-based binning (`label: "${binEnd}t"`); a reader concludes turn intervals are unsupported.
12. `"Limit the number of interval bars displayed (default: 10)."` (line 119) — `bin/wtft.ts:448` — the CLI default is 100 (`opts.hasLimit ? opts.limit : (sessionLimit ?? 100)`, also `:162`, `:367`, `:455`); 10 is only the Pi widget default (`extensions/wtft.ts:112`).
13. `"-w, --width <number>" / "Set the maximum character width of the TUI widget"` (lines 130-131) — `bin/wtft.ts:456,465` — accepted by the parser but never applied in the CLI; chart width is always `Math.min(termColumns - 2*pad, 1023)`.
14. `"(default: 240)"` (line 131) — `bin/wtft.ts:456` — the CLI's effective width default is the terminal width clamped to 1023; 240 is only the Pi widget cap at `extensions/wtft.ts:121`.
15. `"--tz, --timezone <zone>" ... "(default: system local)"` (lines 134-135) — `extensions/wtft.ts:116` — in Pi the default is hardcoded `"America/Los_Angeles"` when no config key exists, not system local (the CLI does default to undefined/local, `bin/wtft.ts:451`).
16. `"-p, --pager" / "Launch an interactive scrollable fullscreen pager overlay"` (lines 146-147) — `bin/wtft.ts:245-247` — in the CLI this prints `❌ Error: -p/--pager is a Pi TUI overlay and is not available in the CLI` and exits 1; the usage entry (unlike the `why` scenario at line 50) does not say Pi-only.
17. `"-S, --show, -H, --hide" / "Show (default) or hide the WTFT cost-auditing widget below the editor."` (lines 154-155) — `bin/wtft.ts` — `showWidget`/`hideWidget` are parsed (`extensions/lib/wtft-cli-shared.ts:160-163`) but never read anywhere in the CLI; the flags are silent no-ops outside Pi.
18. `"--emoji, --no-emoji"` (line 158) — `extensions/lib/wtft-cli-shared.ts:164-167` — `--emojii` and `--no-emojii` (double-i) are also accepted aliases and are undocumented.
19. `"-W, --watch" ... "Ctrl+C to exit."` (lines 162-163) — `extensions/lib/wtft-daemon-lib.ts:823` and `:826` — `q`/`Q` also exit and `r`/`R` restarts the log parser; the footer at `:981` advertises both.
20. Same entry, `"Watch a session file for changes and re-render the bar chart in real-time"` (line 163) — `extensions/lib/wtft-daemon-lib.ts:723-724` — `--watch` refuses to start without a TTY (`❌ --watch requires a real terminal (TTY). Refusing to start.`), which the flag description never mentions.
21. `"--thinking-budget <n>" / "Thinking token budget for utilization percentage display in --tokens output."` (lines 190-191) — `extensions/lib/wtft-cli-shared.ts:197-201` + `extensions/wtft.ts:436` — CLI-only; in Pi the value is ignored and the budget is derived from the current thinking level (`minimal: 1024, low: 4096, medium: 10240, …`).
22. `"--tokens, -C, --cost" ... "--tokens shows a per-model token summary table with utilization."` (lines 194-195) — `bin/wtft.ts:178` + `extensions/lib/wtft-renderer.ts:1036-1037` — `--tokens` also switches the whole bar chart to token units (block-height `▃`/`▇` segments, token tick line) and changes the title to `🔢 WTF Tokens?`; the summary table is only the tail of the output.
23. `"/wtft -t America/New_York"` (line 228) — `extensions/lib/wtft-cli-shared.ts:74-76` — `-t` is explicitly and deliberately unsupported ("Breaking: `-t` and `-T` shortcuts are intentionally NOT supported"); the example arg falls through the parse loop and is silently ignored, so the command renders in the default timezone.
24. Usage list omits `=`-syntax valued flags — `extensions/lib/wtft-cli-shared.ts:235-257` — `--interval=`, `--limit=`, `--width=`, `--tz=`, `--timezone=` are all accepted; a reader of `--help` concludes only space-separated values work.
25. `"cmd": "/wtft --watch"` (line 256) and `"cmd": "/wtft --pad 0"` (line 260) — `extensions/wtft.ts:344-346` — the Pi command handler destructures neither `showWatch` nor `pad`, so both examples are inert under the `/wtft` invocation the examples are written for (they only work as the shell `wtft`).
26. `"...every turn's cost includes the context re-send overhead regardless of category..."` (line 5) — `extensions/lib/wtft-parser.ts:389-410` — `splitOverheadCost` extracts the cache-write re-send cost out of the turn and bills it to a separate `overhead` (or `compaction`) category, so recache turns explicitly do *not* carry it in their work category.
27. `"'q' to exit, using v2.5.1"` (line 90, `--watch` demo) — `extensions/lib/wtft-daemon-lib.ts:194` and `:979-981` — `WTFT_TAGGER_VERSION` is `"2.7.0"` and the footer renders `'q' to exit, using v2.7.0, 'r' to restart`.
28. `"● idle (0:23 to expire)"` (line 108, cache-empty demo) — `extensions/lib/wtft-daemon-lib.ts:511-514` — the rendered form is `idle (cache expires in Nmin)` in whole ceilinged minutes, or `idle (cache emptied)` at zero; no `M:SS` countdown exists.
29. `"WTFT displays 'Cache Empty' (cloud models) or 'No Cache (local model)'"` (line 105) — `extensions/wtft.ts:229-231` — accurate for the Pi widget only; the CLI prints `Daemon started on session … — no data yet` (`bin/wtft.ts:417`) or `No binned data found in session logs.` (`bin/wtft.ts:478`), which the scenario does not mention.
30. Demo bar rows using `░` glyphs, e.g. `"\u001b[38;5;238m░\u001b[0m"` (lines 15, 17, 30, 88) — `extensions/lib/wtft-renderer.ts:1302,1308,374,377` — cost mode emits only `█` and `▌`; `░` is not produced by any code path.
31. Demo colors `38;5;108` (Spec) and `38;5;173` (Code) throughout the `why` demos (lines 15-17, 28-30, 41-42, 88-89) — `extensions/lib/wtft-renderer.ts:58-71` — the palette is `spec: 117`, `code: 179`, `plan: 75`, `research: 141`, etc.; none of the demo color codes match the current table.
32. Every `why` demo omits the legend row — `extensions/lib/wtft-renderer.ts:1070` — a legend row is emitted on every render, unconditionally, directly under the title.
33. Every `why` demo omits the trailing cache line — `extensions/lib/wtft-renderer.ts:1361-1366` — a `CH: N% cache hit (X read / Y total ops)` line is appended whenever any cache/input tokens exist (confirmed in live output).
34. `"── Jul-01 (Claude Code) ────"` (line 40, `--harness` demo) — `extensions/lib/wtft-renderer.ts:1074` — the date label is built as `── ${titleDateStr} ` only; the harness name never appears in the divider or ticks line.
35. Demos show the chart starting at the date/ticks row (lines 14, 27, 39) — `extensions/lib/wtft-renderer.ts:1069` + `bin/wtft.ts:483` — the first CLI line is the dim session path, then the title row with the 24-hour timeline and the `...abcd` session suffix (`:1041`); only the `--watch` demo (line 86) shows the path line.
36. `"-s, --session <path|filter>"` desc (lines 122-123) — `extensions/lib/session-selector.ts:231-255` — omits the non-TTY path: with multiple matches and no TTY it does not show a selector, it prints the top 5 and resolves to the newest session automatically.
37. `"--by-model" ... "~/.config/princess-pi-packages/wtft-pricing.json"` (lines 198-199) — `extensions/lib/wtft-pricing-config.ts:23-26` — accurate, but incomplete: the path is `$XDG_CONFIG_HOME/princess-pi-packages/wtft-pricing.json` when `XDG_CONFIG_HOME` is set.
38. `"--list" / "List all running log parsers with session path, PID, parser version, and idle time."` (lines 166-167) — `bin/wtft-daemon.ts:814` — accurate (`PID … status … v<ver> idle: … <session>`); verified, listed for completeness.

## docs/EXT_WTFT.html

39. `"classifies each historical interaction into one of 9 work types"` (line 50) — `extensions/lib/wtft-parser.ts:30-33` — the `Category` union has 14 members (`plan, spec, research, web, grep, code, tests, git, agents, prompt, compaction, interrupted, overhead, other`), all 14 of which are rendered and legended (`extensions/lib/wtft-renderer.ts:46-49`).
40. Classification table lists 8 rows (lines 117-157) — `extensions/lib/wtft-renderer.ts:57-72` — six categories are entirely absent from the table: `plan` (Plan), `web` (Web), `agents` (Agents), `compaction` (Cmpct), `interrupted` (Waste), `overhead` (Ovrhd).
41. `"If two or more distinct categories are written, the turn is mixed."` (line 85) — `extensions/lib/wtft-parser.ts:630-643` — there is no `mixed` category; multi-category turns resolve by fixed precedence, "latest-workflow-stage-wins": `tests > code > research > spec > plan` on writes, then the same order on reads.
42. `"multiple categories read becomes mixed"` (line 91) — `extensions/lib/wtft-parser.ts:639-643` — same precedence chain applies to reads; `mixed` is never returned.
43. `"█ Spec  ▒ Mixed  █ Code …"` in both example legends (lines 181, 190) — `extensions/lib/wtft-renderer.ts:1059-1062` — the real legend is `Ovrhd Waste Plan Spec Research Web Grep Code Tests Git Agents Prompt Cmpct Other`, in that order, all with `█`; there is no `Mixed` entry and no `▒`/`░` glyph.
44. `"ANSI Bright Green (\x1b[92m)"` for Spec (line 119) — `extensions/lib/wtft-renderer.ts:59` — Spec is 256-color `fg: 117` (light blue); every color code in the table (`\x1b[92m`, `\x1b[38;5;208m`, `\x1b[93m`, `\x1b[95m`, `\x1b[96m`, `\x1b[94m`, `\x1b[37m`, `\x1b[90m`) contradicts `CATEGORY_STYLE` at `:57-72`.
45. `"Treats any .md file as spec."` (line 120) — `extensions/lib/wtft-parser.ts:601-604` — `.md` under `docs/research/` classifies as `plan`, and `extensions/lib/wtft-parser.ts:594-598` classifies `.md` under `node_modules/` as `research`.
46. Code trigger extension list `.ts, .js, .jsonl, .json, .sql, .txt` (line 125) — `extensions/lib/wtft-parser.ts:615` — the actual list also includes `.mjs, .css, .tsx, .jsx, .py, .rs, .go, .sh, .yml, .yaml`.
47. `"Any other commands -> other."` (line 101) — `extensions/lib/wtft-parser.ts:662,670` — a bash command matching `/(?:^|\s)claude(?:\s+-|\s*\||\s*$)/` classifies as `agents`, and it takes priority over `git` and `grep` in the same loop.
48. `"If no commands exist, but dialogue text is present -> prompt."` (line 104) — `extensions/lib/wtft-parser.ts:678` — `prompt` additionally requires `!interaction.unrecognizedTool`; a narrated turn that fired an unmodeled tool falls to `other`.
49. The whole "Classification Taxonomy & Priority Rules" section (lines 80-106) is silent on the tool→category map — `extensions/lib/wtft-parser.ts:90-101,648-651` — `task/agent/workflow → agents`, `websearch/webfetch → web`, `grep → grep`, and `todowrite/taskcreate/taskupdate/taskget/tasklist/askuserquestion/enterplanmode/exitplanmode/skill/toolsearch → plan`, evaluated between file ops and bash commands.
50. Same section is silent on the whole-message override — `extensions/lib/wtft-parser.ts:582` — `if (interaction.interrupted) return "interrupted"` precedes every other rule.
51. Same section is silent on the pre-classified short-circuit — `extensions/lib/wtft-parser.ts:576` — `if (interaction._cat) return interaction._cat`, which is the normal path for every CLI render since the daemon tag file carries the category.
52. `"-t, --tz, --timezone <zone>"` (line 61) — `extensions/lib/wtft-cli-shared.ts:74-76` — `-t` is intentionally unsupported and is silently discarded.
53. `"-w, --width <number> … (default: 240, dynamically scaled down to active terminal columns)"` (line 60) — `bin/wtft.ts:456,465` — the shell binary ignores `-w`; only the Pi extension honors it (`extensions/wtft.ts:458`).
54. The Options list (lines 57-67) omits every flag the CLI actually accepts beyond eight — `extensions/lib/wtft-cli-shared.ts:121-258` — missing: `-s/--session`, `--dir/--cwd`, `--harness`, `-W/--watch`, `--pad`, `-F/--force`, `--tokens`, `-C/--cost`, `--by-model`, `--thinking-budget`, `--emoji/--no-emoji`, `--list`, `--cleanup`, `--restart`, `--stop`, `--version`, `--why`, `--help`.
55. `"Width is always auto-detected from terminal (clamped at 1023). No -w/--width flag."` (line 306) — `extensions/lib/wtft-cli-shared.ts:219-226` — `-w/--width` *is* parsed (it is simply unused by the CLI), and this sentence directly contradicts the same document's own Options entry at line 60.
56. `"characters are plotted in the reverse priority of the legend (from right to left, i.e., Other -> Spec) ensuring that more critical categories like Spec always overlay"` (line 173) — `extensions/lib/wtft-renderer.ts:1295-1309` — bucket mode is a two-pass, cost-ranked collision resolution: entries at a position are sorted by cost descending, the top-cost category becomes the foreground and the second becomes the background of a `▌`; legend order plays no part in overlay priority.
57. `"placing exactly one colored marker block at its absolute visual position"` (line 173) — `extensions/lib/wtft-renderer.ts:1296-1309` — two categories can share one cell rendered as `▌` (fg + bg), so a cell is not always one category's marker.
58. `"guaranteeing monotonically non-decreasing bar widths — a category's segment can only grow, never shrink"` (line 172) — `extensions/lib/wtft-renderer.ts:1109-1133` — clamping applies only to categories already holding ≥ 2 half-slots ("Tiny 1-half-slot allocations … are allowed to flicker"), and the excess-trim loop at `:1124-1133` actively decrements segments that overshot; segments can and do shrink.
59. `"Each category's segment is allocated characters from its absolute cumulative cost against the global scale-maximum"` (line 172) — `extensions/lib/wtft-renderer.ts:1097-1107` — allocation is in half-slots (`barWidthCells * 2`), then paired into cells by `renderHalfBlockBar` (`:364-381`); the character is the pair, not the unit of allocation.
60. `"Legend indicators are placed dynamically on the top title row (or on the second row if the terminal width is narrow)"` (line 175) — `extensions/lib/wtft-renderer.ts:1065-1070` — the legend is never on the title row; the comment there states the fixed placement is deliberate, to avoid layout flip-flop when the SURGE badge appears.
61. `"The dollar labels themselves (e.g. $0.00, $scaleMax/2, and $scaleMax)"` (line 176) — `extensions/lib/wtft-renderer.ts:490-499` — five ticks are computed (`0, max/4, max/2, 3max/4, max`), each dropped only if it would overlap a placed label.
62. `"rendered as ─── Mmm-DD ─── leading directly into the dollar scale line"` (line 176) — `extensions/lib/wtft-renderer.ts:1074` — the label is `── ${titleDateStr} ` (two leading rules, none trailing) padded with `─` to `prefixWidth`.
63. Section is silent on token-mode ticks — `extensions/lib/wtft-renderer.ts:1078-1080,414-474` — with `--tokens` the tick line comes from `buildTokenTickLine`, which forces one decimal on every label (`0.0`, `25.0k`) so the decimal points align.
64. `"Normalizes the absolute length of each interval's ASCII bar … (typically capped at 30-40 characters wide to fit standard terminal outputs gracefully)"` (line 201) — `extensions/lib/wtft-renderer.ts:1013-1020` — bar width is `max(width,40) - prefixWidth - tickReserve` with no 30-40 cap; at 240 columns the bar is ~200 cells.
65. `"tag file (wtft-tags/*.tag.v2.3.4.jsonl)"` (line 199) — `bin/wtft.ts:393` and `extensions/lib/wtft-daemon-lib.ts:194` — the filename is `<session>.jsonl.wtft-tag.v2.7.0.jsonl` (infix `wtft-tag`, not `tag`; version `2.7.0`, not `2.3.4`).
66. `"In Pi, the widget reads directly from ctx.sessionManager.getEntries() (in-memory)"` (line 199) — `extensions/wtft.ts:146-150,177-181` — the widget reads the daemon's classified tag file via `readClassifiedTagFile(getTagPath(sessionFile))`; the comment there says "single source of truth (#92)". `getEntries()` is not called.
67. Same sentence, `"but also spawns a log parser on session_start"` (line 199) — `extensions/wtft.ts:281-287` — accurate; verified.
68. `"When a Claude Code session log is targeted, WTFT auto-detects if a subagent directory exists"` (line 202) — `extensions/lib/wtft-parser.ts:693-700` — subagent rollup also runs for Pi sessions via the `parentSession`-header sibling pattern (Pattern 2), and the extension calls it unconditionally (`extensions/wtft.ts:153`).
69. `"Users/agents can target any older session easily by running wtft -s <number>."` (line 206) — `bin/wtft.ts:276-291` + `extensions/lib/session-selector.ts:252-253` — a numeric `-s` argument is treated as a substring filter, not an index; the non-TTY hint printed by the code is `Run 'wtft -s <substring>' to target a specific session by path or basename filter.`
70. `"prints a clean, numbered list of the top candidates"` (line 206) — `extensions/lib/session-selector.ts:239` — the non-TTY list is hard-capped at 5 (`Math.min(candidates.length, 5)`), while the interactive selector shows 10 (`:260`).
71. The 5-row selector sample (lines 209-213) — `extensions/lib/session-selector.ts:247-250` — omits the trailing tag-status column (`unparsed` or `v<old-version>`) that `formatTagSuffix` (`:207-211`) appends to every row.
72. `"Claude Opus 4 / Claude Sonnet 4 / Claude Haiku 4.5"` pricing rows (lines 226-228) — `extensions/lib/wtft-cost.ts:101-112` — the registry keys are `claude-opus-5/4-8/4-7/4-6/4-5/4-1`, `claude-sonnet-5/4-6/4-5`, `claude-haiku-4-5`; there is no `claude-opus-4` or `claude-sonnet-4` entry (they resolve only by fuzzy substring, `:219-223`).
73. Pricing table omits models entirely (lines 226-230) — `extensions/lib/wtft-cost.ts:101-102,116-130` — `claude-fable-5` and `claude-mythos-5` ($10/$50/$1.00/$12.50) and the tiered `gpt-5.4`, `gpt-5.5`, `gpt-5.6-sol`, `gpt-5.6-terra` entries are priced by the tool but undocumented.
74. Pricing table has no cache-write column (lines 219-231) — `extensions/lib/wtft-cost.ts:101-112` — every Claude entry carries a `cacheWrite` rate (1.25× input) and `extensions/lib/wtft-cost.ts:95-97` notes the 1h-TTL rate is derived as 2× input; cache-write spend is a real, unlisted cost component.
75. `"Peak-valley surge pricing (2×) applies during UTC 01:00–04:00 and 06:00–10:00"` (line 233) — `extensions/lib/wtft-cost.ts:66-79` — accurate (minutes 60-240 and 360-600, end-exclusive); verified.
76. Table is silent on GPT tiering — `extensions/lib/wtft-cost.ts:116-130` + `resolveTieredRates` — rates double above 272K total input tokens; a reader concludes pricing is flat per model.
77. `"All WTFT settings are persisted in harness-agnostic JSON config files"` → `"~/.config/princess-pi/wtft.json"` (line 244) — `extensions/lib/config.ts:21,119-126` — the directory is `princess-pi-packages`; `princess-pi` is a read-only migration fallback (`:128-135`) and `writeConfig` never writes there (`:222`).
78. `"Project-local config: ./.princess-pi/wtft.json"` (line 245) — `extensions/lib/config.ts:121` — the local path is `./.princess-pi-packages/wtft.json`.
79. The 4-level Config Hierarchy list (lines 242-247) — `extensions/lib/config.ts:141-159,190-193` — omits the walk-up: every ancestor directory from CWD to `/` is scanned for `.princess-pi-packages/wtft.json`, closest winning.
80. Same list (line 244) — `extensions/lib/config.ts:181` — omits `XDG_CONFIG_HOME`, which overrides `~/.config` when set.
81. `"Code defaults — hardcoded (interval=1h, limit=10, mode=cumulative, showTicks=true, timezone=America/Los_Angeles, disabledEmoji=false)"` (line 243) — `bin/wtft.ts:162,448,455` — the CLI's hardcoded limit is 100 and its timezone default is `undefined` (system local, `:451`); the quoted defaults hold only for the Pi extension (`extensions/wtft.ts:112,116`).
82. Config Format sample (lines 251-258) — `bin/wtft.ts:165,178` and `extensions/wtft.ts:118` — omits the `tokens` boolean key, which persists token-vs-cost unit mode and is read by both callers.
83. `"● reading... — CLI startup placeholder, shown before the first health check completes (500ms)"` (line 275) — `extensions/lib/wtft-daemon-lib.ts:950,1096` — accurate; verified.
84. `"● idle (3:22 to expire) — no new data for 2m2s"` (line 277) — `extensions/lib/wtft-daemon-lib.ts:511-514` — the rendered string is `idle (cache expires in 4min)`; there is no `M:SS` form. The 2m2s threshold is correct (`IDLE_THRESHOLD_MS = 122_000`, `:388`).
85. `"(Claude: 5 min, DeepSeek: 1 hr, local: no countdown)"` (line 277) — `extensions/lib/wtft-daemon-lib.ts:407-441` — also Gemini (1h), GPT/o1/o3 (30min) and together/fireworks/openrouter (30min); the parenthetical reads as an exhaustive list.
86. `"● restarting... — log parser is being restarted"` (line 281) — `extensions/lib/wtft-daemon-lib.ts:495-497` — when `restarting` is true the code renders `● starting...`; the string `restarting...` appears nowhere (the same wrong string is repeated in the function's own docstring at `:490`).
87. Status list omits a state (lines 275-282) — `extensions/lib/wtft-daemon-lib.ts:492-494` — `● waiting for session .jsonl...` is rendered whenever the daemon is polling for a session file that does not yet exist (`extensions/lib/wtft-cli-shared.ts:360,373`).
88. Status list omits a state (lines 275-282) — `extensions/lib/wtft-daemon-lib.ts:520-523` — a bare `● idle` is rendered when the model is unknown (`cacheTtlMs === undefined`), distinct from `idle (local model)`.
89. `"60-second backup timer / ≤ 60s / CLI --watch (fallback if inotify misses)"` (line 301) and `"The 60-second timer is a safety net"` (line 304) — `extensions/lib/wtft-daemon-lib.ts:732-742` — the fallback is a 1334 ms watchdog (`HEALTHY_BEAT_MS = 1334; // 2 × 667ms daemon poll cycle`); no 60-second timer exists in watch mode. (`extensions/wtft.ts:294-302` has a 60 000 ms timer, but it drives the *Pi widget*, which the table attributes to a "~2-5s widget refresh" instead.)
90. `"Pi widget render timer / ~2-5s (widget refresh)"` (line 300) — `extensions/wtft.ts:294-302` — the widget's own timer is `setInterval(…, 60000)`; other refreshes are event-driven on `agent_settled` and `session_tree` (`:315-328`), not a 2-5s tick.
91. `"press r to restart the log parser. The footer shows `r to restart log parser` in red when the parser is stopped"` (line 307) — `extensions/lib/wtft-daemon-lib.ts:979-981` — the footer text is `'r' to restart` (quoted, shorter), it is only emitted when `settings.daemonPath` is set, and the full line is `'q' to exit, using v2.7.0, 'r' to restart`.
92. `"a ── Cache Miss ── divider line with tick-position ┼ markers is drawn on that bin"` (line 164) — `extensions/lib/wtft-renderer.ts:1170-1188,1202-1204` — accurate, including "always shown, even with `--no-ticks`" and the turn-based-interval case; verified.
93. `"cache_read_input_tokens 0 with cache_creation_input_tokens above 0"` (line 164) — `extensions/lib/wtft-parser.ts:234-236` — accurate; verified.
94. `"tag field miss, tagger v2.7.0"` (line 166) — `extensions/lib/wtft-daemon-lib.ts:194` — accurate, and inconsistent with the same document's `v2.3.4` at line 199 (see finding 65).
95. `"Non-DeepSeek models render an all-green timeline with no badges."` (line 167) — `extensions/lib/wtft-renderer.ts:1052-1056,732` — accurate for hue, but incomplete: the current hour is always bold and always a clock-face emoji, and noon is always `☀️`, regardless of model.
96. `"The Pi widget passes the session model explicitly; the CLI auto-detects DeepSeek from classified interactions."` (line 167) — `extensions/wtft.ts:216-219` and `extensions/lib/wtft-renderer.ts:1046-1051` — accurate; verified.
97. `"Costs are rounded to 6 decimal places (micro-cent precision)"` (line 199) — `extensions/lib/wtft-daemon-lib.ts:51,69` — accurate; verified.
98. `"If the daemon dies from idle timeout (24h), the agent_end handler checks health via checkDaemonHealth and auto-revives it"` (line 199) — `extensions/wtft.ts:330-336` → `extensions/lib/wtft-cli-shared.ts:321-338` — accurate; verified.
99. Example 1 and Example 2 title rows `💸 WTF Tokens?  🌒────────☀️───────🕚🌒 ⚡ SURGE 2x` (lines 180, 189) — `extensions/lib/wtft-renderer.ts:1041-1042` — omit the dim `...abcd` session suffix that is appended to the title whenever `sessionNameSuffix` is passed (always, from `bin/wtft.ts:470` and `extensions/wtft.ts:222`).
100. Both examples omit the trailing `CH:` cache line and the token-mode footer (lines 179-195) — `extensions/lib/wtft-renderer.ts:1344-1366` — the cache-efficiency line is emitted in both units, plus a `▃ cached/carryover ▇ new/uncached $ = cost-only (web tools)` key bar and an `↑/↓/R/CH` summary in token mode.
101. Document is silent on the "Other" bloat warning — `extensions/lib/wtft-renderer.ts:1329-1341` — a `⚠️ "Other" category: N% of session cost … Run wtft --other to drill down.` line is injected whenever Other exceeds 20% and $6.00.
102. Document is silent on the unpriced-model marking — `extensions/lib/wtft-renderer.ts:1611-1625,1661-1663` and `bin/wtft.ts:504-510` — costs for models missing from the registry are suffixed `?` with a footnote (`priced at default $3/$15 rates`) plus a stderr warning per unknown model.
103. `"Under the Pi TUI Widget, this [--other] prints the histogram directly into the prompt-response message scroll (via chat notification)"` (line 72) — `extensions/wtft.ts:422-423` — accurate; verified. `"Under the CLI binary, it appends the histogram to stdout directly below the printed timeline"` — `bin/wtft.ts:485-492` — accurate; verified.
104. `"descending-frequency histogram of the primary executable/command names"` (line 72) — `extensions/lib/wtft-renderer.ts:1494-1501,1517` — commands are sorted by count descending *within* semantic groups, and the groups themselves are ordered by a fixed `groupOrder` list with `Unclassified` last; the output is not a single frequency-ranked list.
105. `"shipped as plain ESM JavaScript (bin/wtft.mjs) so it can be installed globally"` (line 77) — confirmed present at `bin/wtft.mjs` and executed by the test at `tests/wtft-title-layout.test.ts:96`; accurate.
106. `"Proposed Verification Plan … Step 1 (Mock Log Generation) … Step 3 (Color rendering check)"` (lines 264-269) — `tests/` — describes a manual plan that the repo has since superseded with executable suites (`tests/wtft-title-layout.test.ts`, `tests/wtft-half-block.test.ts`, `tests/wtft-issue-52-tool-categories.test.ts`, and ~25 others); the section documents no current verification.

## Glossary check (CONTEXT.md)

`CONTEXT.md` has exactly one vocabulary section, `## Language — Serve` (line 5), whose entries and `_Avoid_` rulings are scoped to the `serve` tool's internals (the file states this explicitly at lines 7-10). **There is no wtft/WTFT section and no `_Avoid_` list that governs this tool**, so I applied none of serve's avoided terms to wtft's strings and invented no replacement vocabulary.

One term-consistency observation that is a code-vs-artifact fact rather than a glossary ruling:

107. `"A persistent background daemon (wtft-daemon)"` (`docs/EXT_WTFT.html:199`) vs the same document's `"Log Parser Health Monitoring"` heading (line 271) — `extensions/lib/wtft-cli-shared.ts:345` (`"log parser not started"`), `bin/wtft.ts:397` (`❌ wtft-daemon not found`) — user-facing strings mix both names for one process, and the docs mirror the split; nothing in `CONTEXT.md` adjudicates which is correct.

## Not changed

Nothing was fixed — this is audit output only. The six failing assertions in `tests/wtft-title-layout.test.ts` are live now (`node --experimental-strip-types tests/wtft-title-layout.test.ts` → `26 passed, 6 failed`), so the suite is red in its current state.
