Read the hook in full (966 lines), then the two TS files, spec, CLAUDE.md, README, and CONTEXT.md. Every finding below was verified by running `hooks/block-dangerous-git.sh` against a throwaway repo (`/tmp/gtest`, `main` + worktree `42-foo`) with hook-shaped JSON on stdin.

---

## A. The source's own banners and docstrings, in file order

1. **`:3` — "Block dangerous git commands — branch-aware AND push-target-aware (#70, #74)"** → the body cites #225, #249, #301, #303, #305 (`:5`, `:11`, `:49`, `:844`); the header issue list stops at #74, so a reader dates the file three redesigns early.
2. **`:4` — "Always block: checkout ., restore ., clean -f/-fd, worktree remove --force/-f"** → `hooks/block-dangerous-git.sh:840` and `:861` match `-*f*`, any short-flag cluster containing `f`; measured `git clean -fd` → exit 2, and `-ff`, `-xf`, `-df` block identically. "`-f/-fd`" reads as an exhaustive pair.
3. **`:6-8` — "teardown is meant to be confirm-first per `~/git-projects/CLAUDE.md` § Worktree Teardown"** → `docs/dev-workflow-spec.md:478-480` records that rule as relaxed ("merging a branch's PRs is already the authorization to remove its worktree — no separate confirmation is asked for that"). The banner's stated rationale is stale.
4. **`:9-14` — "Block on main/master only: … branch -D main/master"** → `hooks/block-dangerous-git.sh:750-777` never calls `effective_branch`; measured from branch `99-feat`, `git branch -D main` → exit 2. Force-deleting main is blocked on **every** branch, not only on main.
5. **`:13` — "Allowed there: … every --abort/--quit"** → `hooks/block-dangerous-git.sh:816` exempts `--abort`/`--quit` only for `merge|rebase|cherry-pick|am`; measured `git pull --abort` → exit 2, `git commit --abort` → exit 2.
6. **`:14` — "checkout -b / switch -c (the escape …)"** → `hooks/block-dangerous-git.sh:260` and `:262` also accept `-B`, `--orphan`, `-C`, `--create`, `--force-create`; measured `git switch --force-create 42-foo && git commit -m x` → exit 0. A reader concludes `--create`/`--orphan` are unsupported.
7. **`:25-26` — "inspect each `git … push` sub-command's refspec tokens, and resolve the branch from `-C <path>` when present"** → `hooks/block-dangerous-git.sh:724-725` also captures `--git-dir` (and `:87-89` says so), and `:210-245` resolves the branch from `cd`/`pushd` line-state. The "Why" paragraph names only `-C`.
8. **`:35-36` — "tests/git-guardrails-parity.test.ts runs the same fixture against both"** → true (`tests/git-guardrails-parity.test.ts:21,25` loads both), which makes `docs/dev-workflow-spec.md:539` ("still exercises `block-dangerous-git.sh` only") the false half of the pair. Listed again under D.
9. **`:39-43` — no claim, and that is the finding** → `hooks/block-dangerous-git.sh:40` hard-depends on `jq`; with `jq` off `PATH` the hook prints `jq: command not found`, `COMMAND` is empty, `:42` exits 0, and measured `git push origin main` sails through. The guard **fails open** on a missing dependency, and no banner, docstring, or doc says so.
10. **`:44` — no claim** → `hooks/block-dangerous-git.sh:44` accepts the cwd at either `.tool_input.cwd` **or** top-level `.cwd`; no artifact documents the second accepted shape.
11. **`:54-55` — "A double-slash path is never a real directory, so it rides through the scope/snapshot plumbing as an ordinary cwd value"** → true of the cwd plumbing, but `hooks/block-dangerous-git.sh:140` runs `realpath -sm` on it; measured `realpath -sm //unknown` → `/unknown`. The sentinel does *not* ride through `repo_key` unchanged.
12. **`:84-85` — "A relative -C is what git would see from the TOOL-CALL cwd — resolve it there"** → `hooks/block-dangerous-git.sh:92-93` resolves against `$HOOK_CWD`, the **effective** cwd that `apply_cd` mutates, not `ORIG_CWD`; measured `cd .claude/worktrees/42-foo && git -C . commit -m x` → exit 0 (resolved in the worktree, not the tool-call clone). The term "TOOL-CALL cwd" is the pre-#301 one.
13. **`:87-89` — "git resolves --git-dir relative to the directory the -C chain established"** → `hooks/block-dangerous-git.sh:100` runs `GIT_DIR="$gitdir" git branch --show-current` with **no `-C`**, i.e. from the hook process's own cwd; only the path *join* at `:97-99` honours the chain. `extensions/lib/git-guardrails-core.ts:56-59` passes `cwd: dir || "."`. The twins diverge under the `:35` "keep logic in sync" claim.
14. **`:128-130` — "the key matches the TS twin's `path.resolve()` byte for byte"** → `hooks/block-dangerous-git.sh:140` always normalises; `extensions/lib/git-guardrails-core.ts:158` returns the raw `cwd` when no `-C` is present. For the UNKNOWN sentinel the keys are `/unknown|` (bash) vs `//unknown|` (TS) — not byte for byte.
15. **`:118-122` — "`checkout -b|-B|--orphan` … mark the repo they ran in as being on `<name>`"** → `hooks/block-dangerous-git.sh:277` refuses the lift when the ref already exists and no force flag is present; measured `git checkout -b 42-foo && git commit -m x` → exit 2. The exception is stated 150 lines later at `:270-272`, not here.
16. **`:162` — expand_word "returns 1 when anything is left unresolved — command substitution, an unknown variable, a glob"** → `hooks/block-dangerous-git.sh:165` and `:178` test only backticks, `$(`, and a leftover `$`. There is no glob test; measured `cd /tmp/gt* && git commit -m x` from main → blocked with the plain **on main/master** message, not the UNKNOWN message, proving the glob returned 0. `extensions/lib/git-guardrails-core.ts:114-119` omits "a glob" — the `.sh` docstring is the wrong one.
17. **`:199` — "A branch switch inside it DID happen on disk, so LIFT_* is left as the child set it"** → `hooks/block-dangerous-git.sh:203` restores `LIFTS="$saved_lifts"` on the very next line; measured `bash -c "git checkout -b zz9" && git commit -m x` → exit 2. The docstring contradicts its own three-line body.
18. **`:199` — "LIFT_*"** → no such variable exists; `hooks/block-dangerous-git.sh:59` declares `LIFTS`. Stale identifier from a pre-#305 revision (`:47` carries the same `LIFT_*` name).
19. **`:206-208` — "Returns 0 (and updates HOOK_CWD) when it consumed the sub-command"** → `hooks/block-dangerous-git.sh:215` (`pushd -n`), `:221` (`cd a b`), `:239` (relative from unknown) and `:243` (non-existent target) all return 0 **without** touching `HOOK_CWD`. The parenthetical states an invariant the function does not hold.
20. **`:284-285` — ref_exists "Same -C/--git-dir resolution as branch_of"** → `hooks/block-dangerous-git.sh:296` uses `git -C "${dir:-.}"` where `branch_of:104-107` falls back to a bare `git` with no `-C`. Same intent, not the same code path.
21. **`:472-475` — "Push-target parsing (#74): inspect each git sub-command's tokens. One blocked sub-command blocks the whole command line (fail-safe)."** → the second sentence describes the walk at `hooks/block-dangerous-git.sh:928-960` and `block()`'s `exit 2` at `:72`, not `check_push`, the function it sits above.
22. **`:485-487` — no artifact carries this** → `hooks/block-dangerous-git.sh:486-487` blocks `--all`, `--branches`, `--mirror` outright; measured `git push --mirror origin` → exit 2. Documented nowhere outside this comment.
23. **`:520-523` / `:525-532` — no artifact carries these either** → `git push origin :` → exit 2 (matching refspec) and `git push origin HEAD` → exit 2 (HEAD/`@` resolved to the current branch). Both are real, both are undocumented outside the source.
24. **`:680-683` — `eval` has no comment at all** → `hooks/block-dangerous-git.sh:682` calls `check_command_string` with **no save/restore**, so eval shares line-state; measured `eval "git checkout -b zzeval" ; git commit -m x` → exit 0, while the `bash -c` equivalent blocks. `extensions/lib/git-guardrails-core.ts:577` explains this ("eval runs in the SAME shell: its cd and lifts persist"); the `.sh` twin does not.
25. **`:804-807` — "Skip the word after any argument-taking option"** → `hooks/block-dangerous-git.sh:813` lists `-S`, which git spells attached-only (`-S[<keyid>]`), so the list over-skips. `extensions/lib/git-guardrails-core.ts:603-604` states why that is safe ("Over-skipping is safe … under-skipping is the fail-open case"); the `.sh` carries no such note.
26. **`:870-873` — "Check for dangerous gh (GitHub CLI) commands" (plural)** → `hooks/block-dangerous-git.sh:878` tests exactly one shape, `pr merge`. Everything else `gh` does is unguarded.
27. **`:878` — positional adjacency, unstated** → `T[s+1]`/`T[s+2]` must be literally `pr`/`merge`; measured `gh -R o/r pr merge 5` → **exit 0**, `gh --repo o/r pr merge 5` → **exit 0**. Same hole in `extensions/lib/git-guardrails-core.ts:878`.
28. **`:884-889` — "Full check of one command string: strip heredocs, inspect command substitutions, quote-aware split … This is the recursion point for nested command strings"** → this describes `check_command_string` (`hooks/block-dangerous-git.sh:917`), but it sits above `check_one_sub` (`:891`), which is separately documented by the one-liner at `:890`. **The banner is attached to the wrong function**, and the function it describes already has its own correct banner at `:908-916`.
29. **File mode** → `hooks/block-dangerous-git.sh` is `-rw-rw-r--` while its two tracked siblings are `-rwxrwxr-x`. Not a spec contradiction (`bin/install-workflow-tools:337` runs `chmod +x` on deploy), but the canonical copy of the file whose spec section is titled "is it **executable**" is the one without the bit.

## B. `extensions/git-guardrails.ts`

30. **`:6` — "Always block: checkout ., restore ., clean -f (discard work, any branch)"** → `extensions/lib/git-guardrails-core.ts:781-787` also blocks `worktree remove --force`; the `.sh` twin lists it at `:4`. The TS header dropped it.
31. **`:11` — "every --abort/--quit"** → `extensions/lib/git-guardrails-core.ts:751` gates on `UNDOABLE.has(cmd)` = `merge|rebase|cherry-pick|am` only. Same wrong claim as A5.
32. **`:12` — "checkout -b / switch -c (the escape …)"** → `extensions/lib/git-guardrails-core.ts:622-624` accepts `-B`, `--orphan`, `-C`, `--create`, `--force-create`. Same omission as A6.
33. **`:1-46` — the header never mentions the `gh` gate** → `extensions/lib/git-guardrails-core.ts:877-881` blocks `gh pr merge` in this extension's own path (`checkOneSub:869`). The largest single behaviour the extension enforces is absent from its 46-line header.
34. **`:1-46` — JSDoc placement** → the `/** … */` block is followed by `import type { ExtensionAPI }` at `:48` with no `@fileoverview`/`@module` tag, so TypeScript attaches the whole file overview to the **import declaration**, not the file or the default export at `:76` (which has no docstring at all).
35. **`:57-72` — `logMainBranchSession` / `AX_LOG`** → appends to `~/.pi/agent/logs/main-branch-sessions.jsonl` on every session started on main. `grep` over `docs/`, `README.md`, `CLAUDE.md`, `CONTEXT.md` returns nothing for `main-branch-sessions`. An undocumented file written outside the repo.
36. **`:92` — `pi.appendEntry("main-branch-warning", …)`** → writes a custom entry into the session transcript; likewise undocumented in every artifact.

## C. `extensions/lib/git-guardrails-core.ts`

37. **`:2` — "(#70, #74, #301)"** → the file also carries the gh gate (`:461-466` cites #208/#189) and the worktree-remove guard (`:774-780` cites #225 gap 2). Issue list is two features short.
38. **`:1-9` — JSDoc placement** → followed by `import { execSync }` at `:11` with no `@fileoverview`, so the module overview binds to the import. Same class as B34.
39. **`:202-207` — "Drop heredoc bodies so quoted text like `<<EOF\ngit push origin main\nEOF` is never mistaken for a command"** → the banner sits above `const HEREDOC_DELIM_CHARS` at `:208`; the function it describes, `stripHeredocs`, starts at `:210`. In the `.sh` twin the same banner (`:300-309`) does sit above `strip_heredocs`.
40. **`:45-46` — "A relative -C is what git would see from the TOOL-CALL cwd"** → `branchOf` is called from `effectiveBranch:168` with `st.cwd`, the effective cwd mutated by `applyCd:194`. Same stale term as A12.
41. **`:801-808` — checkChild: "substitutions are inspected before the walk, so a lift from one would apply to sub-commands that ran EARLIER"** → the stated protection does not hold, because `splitOutsideQuotes:406-408` **also** splits on the `(`/`)` of `$( … )` and `st.lifts` is deliberately not scoped (`:836-840` restores only cwd and vars). Measured on the `.sh`: `$(git checkout -b zz6) ; git commit -m x` → **exit 0**, while the backtick form `` `git checkout -b zz7` ; git commit -m x`` → exit 2.

## D. `docs/dev-workflow-spec.md`

42. **`:389-392` — "`$( … )` / `bash -c` bodies, pipeline elements and backgrounded jobs are child shells and *nothing* they set carries back — not even a branch switch"** → measured `$(git checkout -b zz6) ; git commit -m x` → **exit 0** and `(git checkout -b zz5) ; git commit -m x` → **exit 0**, per `hooks/block-dangerous-git.sh:600` (parens split into scope markers) and `:65-66` ("Lifts are not scoped"). `bash -c`, pipelines and `&` do hold; `$( … )` does not.
43. **`:454` — "`git rebase --onto origin/main origin/218-base …` → exit 0 allowed"** → measured from a repo on main, **exit 2** (`hooks/block-dangerous-git.sh:803`, #301 put `rebase` in the commit-like set); exit 0 only from a feature branch. The neighbouring rows at `:455` and `:459-460` carry a "(from a repo on main)" annotation; this one does not, and its verdict now depends on it.
44. **`:539` — "`tests/git-guardrails-parity.test.ts` still exercises `block-dangerous-git.sh` only"** → `tests/git-guardrails-parity.test.ts:21` imports `checkGitCommand` from the core and `:6-8` states it runs both. Contradicted by the hook's own banner at `hooks/block-dangerous-git.sh:35-36`.
45. **`:337` — "nothing hooks `git worktree remove`"** → `hooks/block-dangerous-git.sh:851-865` hooks it; measured `git worktree remove -f <path>` → exit 2, and the spec's own table at `:457` says so. True only of the unforced form.
46. **`:373-374` — "judged per sub-command with **two** pieces of line-state"** → `hooks/block-dangerous-git.sh` carries four: `HOOK_CWD` (`:48`), `LIFTS` (`:59`), `LINE_VARS` (`:63`) and the `CWD_STACK`/`VARS_STACK` scope stack (`:67-68`). The spec describes the vars at `:382-383` without counting them.
47. **`:384-386` — "unknown is protected until a resolvable `cd`, `cd -` or `popd` restores it"** → `hooks/block-dangerous-git.sh:147` also lets an **absolute `-C`** out; measured `cd "$NOPE_XYZ" && git -C /tmp/gtest/.claude/worktrees/42-foo commit -m x` → exit 0 with the cwd still unknown. A fourth escape the sentence excludes.
48. **`:506-508` — "`gh pr merge` in any form is human-only, regardless of flags — and since #249 that is a technical block"** → measured `gh -R o/r pr merge 5` → **exit 0** and `gh --repo o/r pr merge 5` → **exit 0** (`hooks/block-dangerous-git.sh:878`). "Regardless of flags" is false for any flag placed between `gh` and `pr`.
49. **`:361-365` + `:449-464` — the guardrails bullet and its measured table** → never mention `reset --hard` (`:739-749`), `branch -D` (`:750-777`), `checkout .` / `restore .` (`:784-792`), or `git clean -f` as blocked operations; `clean -f` appears only as a passing analogy at `:474`. All four measured blocking: exit 2.
50. **`:449-464` — the measured table** → omits `push --all|--branches|--mirror` (`:486`), the `:` matching refspec (`:523`), and `push origin HEAD` (`:531`), all measured exit 2. A reader taking the table as the block list concludes these are allowed.
51. **`:519-525` — "Getting the hooks onto the host"** → silent on the hook's `jq` runtime dependency and on the fact that a missing `jq` makes it exit 0 (`hooks/block-dangerous-git.sh:40-43`). The section's whole subject is "a hook it cannot run gates nothing"; the dependency that produces exactly that state is unmentioned.
52. **`:357-447` — the guardrails section** → never mentions `eval`, though `hooks/block-dangerous-git.sh:680-683` recurses through it **sharing line-state**, unlike every other nesting construct the section does enumerate. Measured `eval "git checkout -b zzeval" ; git commit -m x` → exit 0.
53. **`:357-447`** → never mentions the wrapper set at `hooks/block-dangerous-git.sh:544` (`command env nice nohup time timeout stdbuf setsid ionice sudo doas exec`); `:512-513` measures only `sudo` and a `GH_HOST=` prefix. Measured `timeout 5 gh pr merge 1` → exit 2 and `exec gh pr merge 1` → exit 2 — behaviour a reader has no way to predict from the doc.
54. **`:375` — "`cd`/`pushd` move the effective cwd for the rest of the line (`cd -`/`popd` reset it)"** → silent on `pushd -n` (`hooks/block-dangerous-git.sh:215`), which consumes the sub-command and moves nothing.
55. **`:373-403` — the #301 bullet** → names `-C` as the repo selector throughout but never `--git-dir`, which `hooks/block-dangerous-git.sh:724-725` captures for the same purpose.
56. **`:409-415` — the Pi twin description** → covers only the `tool_call` Edit/Write gate; silent on the `session_start` main-branch warning at `extensions/git-guardrails.ts:85-102`, which is the twin's third registered handler.

## E. `CLAUDE.md`

57. **`:7` — "**Never edit on `main`.** Always use `<issue#>-<slug>` branches"** → stated as convention only. `hooks/block-dangerous-git.sh:803-832` makes `commit`, `merge`, `rebase`, `cherry-pick`, `am` and `pull` on main a technical block (exit 2). The repo's own Hard Gates section never says the gate is enforced, or by what.
58. **`:30-32` — "guardrail hooks (`hooks/`) → `~/.claude/hooks/`"** → the only mention of the guardrails in the whole file; `block-dangerous-git.sh` is never named, and none of its behaviour (`gh pr merge` blocked, commits on main blocked, `worktree remove --force` blocked) reaches this file.
59. **`:44` — "`pr-merge …` Squash-merges that branch's PR … (**human-only**)"** → `hooks/block-dangerous-git.sh:878` blocks the raw `gh pr merge`, but `pr-merge` is an opaque script name that `skip_benign_prefix:692` returns 1 on, so it is **not** blocked. The bold "human-only" reads as enforced; for this script it is convention (spec `:485-494` calls the gap permanent and deliberate).

## F. `README.md`

60. **`:27-35` — "📦 What's Included" extension table** → lists `wtft.ts`, `ddg-search.ts`, `serve.ts`, `github-issue-autocomplete.ts`, `system-clock.ts`. `extensions/git-guardrails.ts` exists, registers three handlers, and is absent. A reader of the front door concludes the repo ships no guardrail.
61. **`:81-85` — "The git-workflow scripts are a separate channel."** → enumerates the eleven `~/bin` scripts but omits the `hooks/` → `~/.claude/hooks/` half of the same installer (`bin/install-workflow-tools:360-361`), which `CLAUDE.md:30-31` does name. `grep -i "hook\|guardrail\|block-dangerous"` over README returns only the unrelated wtft "hooks into turn-completion events" at `:29`.

## G. `CONTEXT.md` glossary

62. **No section exists for this tool.** `CONTEXT.md` carries exactly two glossary sections — `## Language — Serve` (`:5`) and `## Language — WTFT` (`:164`). There is no guardrails, hooks, or git-workflow section, so no `_Avoid_` list governs any string in `block-dangerous-git.sh`, `git-guardrails.ts`, or `git-guardrails-core.ts`. I checked each block reason string (`:487`, `:510`, `:523`, `:531`, `:536`, `:745`, `:774`, `:788`, `:825`, `:829`, `:831`, `:838`, `:859`, `:879`) against both existing sections' `_Avoid_` entries; none of those entries is scoped to this tool, and I am inventing no terminology to fill the gap. **Naming this gap is itself the finding** — the file with the most user-facing strings in the repo has no owned vocabulary.

---

**Nothing fixed.** The highest-severity items, if you want an order to work in: #9 (jq-missing fails open), #27/#48 (`gh -R … pr merge` walks the human-only gate), #42 (`$( … )` lifts leak, contradicting the spec's fail-closed claim), #17 (docstring contradicts its own body), #28 (banner on the wrong function).
