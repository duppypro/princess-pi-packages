---
name: learning_pi_extension_api
description: High-level routing entrypoint for Pi's extension APIs, TUI rendering constraints, and Duppy's custom VPS preferences.
---

# Skill: Pi Extension API, TUI Rendering, & Duppy's Standards

This skill acts as a **lightweight progressive-disclosure router** to manage prompt context efficiently.

> 💡 **Princess-Pi Guideline:** When the user asks a question about how Pi works, how TUI rendering behaves under the hood, or how Duppy prefers to configure the Pi harness, **do NOT answer from memory**. 
> 
> Follow these strict steps:
> 1. Use the `read` tool to load and search the detailed companion file: `skills/learning-pi/API_LESSONS.md`.
> 2. Only if you cannot find the answer in `API_LESSONS.md`, proceed to search the broader Pi documentation (`~/.pi/agent/docs/`) or the web.
